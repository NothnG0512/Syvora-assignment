apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-express-api.fullname" . }}
  labels:
    {{- include "node-express-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "node-express-api.selectorLabels" . | nindent 4 }}